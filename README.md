# Test Case Editor

Редактор тест-кейсов. Приложение для создания, редактирования и управления тест-кейсами в формате JSON.

## 📋 Содержание

- [Установка и запуск](#установка-и-запуск)
- [Основной функционал](#основной-функционал)
- [Инструкции по использованию](#инструкции-по-использованию)
- [Архитектура](#архитектура)
- [Технические детали](#технические-детали)

---

## 🚀 Установка и запуск

### Требования

- Python 3.8+
- PyQt5 >= 5.15.0

### Установка зависимостей

```bash
pip install -r requirements.txt
```

### Запуск приложения

```bash
python run_app_v2.py
```

---

## ✨ Основной функционал

### 📁 Работа с файлами и папками

#### Создание элементов
- **Создание тест-кейса**: ПКМ на папке или корне дерева → "➕ Создать тест-кейс"
  - Новый тест-кейс создается с одним пустым шагом
  - Автоматически генерируется уникальный ID и имя файла
- **Создание папки**: ПКМ на папке или корне → "📁 Создать папку" / "📁 Создать подпапку"

#### Управление элементами
- **Переименование файла**: ПКМ на тест-кейсе → "✏️ Переименовать"
- **Переименование папки**: ПКМ на папке → "✏️ Переименовать"
- **Дублирование тест-кейса**: ПКМ на тест-кейсе → "📋 Дублировать"
- **Удаление файла**: ПКМ на тест-кейсе → "🗑️ Удалить"
- **Удаление папки**: ПКМ на папке → "🗑️ Удалить папку" (удаляет папку со всем содержимым)

#### Перемещение элементов
- **Drag & Drop**: Перетащите файл или папку на целевую папку
- **Массовое перемещение**: Включите режим редактирования (✏️), отметьте элементы чекбоксами, перетащите на папку

#### Открытие в проводнике
- **Открыть файл**: ПКМ на тест-кейсе → "🪟 Открыть в проводнике"
- **Открыть папку**: ПКМ на папке → "🪟 Открыть в проводнике"

### 📝 Редактирование тест-кейсов

#### Основная информация
- **Название тест-кейса**: Редактируется напрямую в поле заголовка
- **ID тест-кейса**: Автоматически генерируется при создании
- **Автор**: Поле для указания автора тест-кейса
- **Владелец**: Поле для указания владельца
- **Ревьювер**: Поле для указания ревьювера
- **Статус**: Выпадающий список (Draft, In Progress, Done, Blocked, Deprecated)
- **Тип теста**: Выпадающий список (manual, automated)
- **Test Layer**: Выпадающий список (Unit, API, UI, E2E, Integration)
- **Severity**: Выпадающий список (BLOCKER, CRITICAL, MAJOR, NORMAL, MINOR)
- **Priority**: Выпадающий список (HIGHEST, HIGH, MEDIUM, LOW, LOWEST)

#### Контекстная информация
- **Epic**: Поле для указания epic
- **Feature**: Поле для указания feature
- **Story**: Поле для указания story
- **Component**: Поле для указания компонента
- **Environment**: Поле для указания окружения
- **Browser**: Поле для указания браузера

#### Описательные поля
- **Теги**: Многострочное поле (каждый тег с новой строки)
- **Описание**: Многострочное поле с автоматической подстройкой высоты
- **Предусловия**: Многострочное поле с автоматической подстройкой высоты
- **Общий ожидаемый результат**: Многострочное поле с автоматической подстройкой высоты

#### Шаги тестирования
- **Добавление шага**: ПКМ на списке шагов → "➕ Добавить новый шаг"
  - При добавлении автоматически прокручивается к новому шагу и устанавливается фокус на поле "Действия"
- **Вставка шага выше**: ПКМ на шаге → "⬆️ Вставить шаг выше"
- **Вставка шага ниже**: ПКМ на шаге → "⬇️ Вставить шаг ниже"
- **Перемещение шага вверх**: ПКМ на шаге → "⇡ Переместить наверх"
- **Перемещение шага вниз**: ПКМ на шаге → "⇣ Переместить вниз"
- **Удаление шага**: ПКМ на шаге → "✕ Удалить"
- **Редактирование шага**:
  - Поле "Действие": Описание действия шага
  - Поле "Ожидаемый результат": Ожидаемый результат шага
  - Оба поля автоматически подстраивают высоту под содержимое

#### Сохранение изменений
- Кнопка "💾 Сохранить" появляется при изменении любого поля
- Сохраняет все изменения в JSON файл
- Обновляет время изменения (`updatedAt`)
- Скрывается после успешного сохранения

### 🔍 Поиск и фильтрация

- **Поиск по дереву**: Поле поиска в левой панели
  - Фильтрует тест-кейсы и папки по введенному тексту
  - Поиск происходит в реальном времени

### 🎯 Режимы работы

#### Режим редактирования (по умолчанию)
- Доступно редактирование всех полей
- Доступны контекстные меню для шагов
- Доступны все панели (Ревью, Создать ТК, JSON Preview, Статистика)

#### Режим запуска тестов
- Переключается через toggle switch в хеддере
- Поля редактирования становятся read-only
- Контекстные меню для шагов недоступны
- Отображаются кнопки статусов шагов (passed ✓, failed ✕, skipped S)
- Автоматически открывается панель статистики

### 🤖 Интеграция с LLM

#### Настройка LLM
- **Выбор модели**: Комбобокс в хеддере
  - Поддерживает фильтрацию при вводе текста
  - Список моделей загружается автоматически с указанного хоста
- **Настройка хоста**: Через файл `settings.json` (поле `LLM_HOST`)

#### Панель ревью
- **Функционал**: Ревью тест-кейсов с помощью LLM
- **Использование**:
  1. Выберите тест-кейс в дереве
  2. ПКМ на тест-кейсе → "📝 Ревью"
  3. Введите промт для ревью (или используйте сохраненный)
  4. Нажмите Enter или кнопку отправки
  5. Получите ответ от LLM
- **Сохранение промта**: Промт автоматически сохраняется в настройках
- **Прикрепление файлов**: Автоматически прикрепляется методика и текущий тест-кейс

#### Панель создания тест-кейсов
- **Функционал**: Создание тест-кейсов с помощью LLM
- **Использование**:
  1. Откройте панель "Создать ТК"
  2. Прикрепите файлы с техническим заданием (опционально)
  3. Введите задачу для создания тест-кейсов
  4. Нажмите Enter или кнопку отправки
  5. LLM создаст тест-кейсы в формате JSON
  6. Тест-кейсы автоматически сохраняются в папку "from LLM"
- **Сохранение промта**: Промт автоматически сохраняется в настройках

### 📊 Статистика и управление раннером

#### Панель статистики
- **Отображение статистики**:
  - Всего тест-кейсов
  - Без статуса прохождения (все шаги пустые или pending)
  - Все шаги passed (зеленым цветом)
  - Есть failed (красным цветом)
  - Есть skipped (серым цветом)

#### Управление всеми тест-кейсами
- **Сбросить все статусы**: Сбрасывает статусы всех шагов во всех тест-кейсах до пустого значения
- **Generate Allure**: Генерирует Allure отчеты
  - Создает папку `Reports/YYYY_MM_DD_HH_MM/`
  - Конвертирует все тест-кейсы в формат Allure Test Result JSON
  - Копирует JSON файлы в папку отчета
  - Автоматически открывает проводник в созданной папке
  - Готовые файлы можно использовать с командой `allure serve <путь>`

#### Управление текущим тест-кейсом
- **All PASS**: Помечает все шаги текущего тест-кейса как "passed"
- **Сбросить статусы текущего**: Сбрасывает статусы всех шагов текущего тест-кейса

### 📄 JSON Preview

- **Функционал**: Просмотр JSON представления текущего тест-кейса
- **Особенности**:
  - Синтаксическая подсветка (ключи и значения разными цветами)
  - Автоматическое обновление при изменении тест-кейса
  - Тема "midnight" для удобного чтения

### 🔧 Дополнительные функции

#### Импорт из Azure DevOps
- **Меню**: Файл → "Импорт из ALM"
- **Функционал**: Импорт тест-кейсов из JSON файлов Azure DevOps
- **Результат**: Создаются тест-кейсы в папке "from alm"

#### Git интеграция
- **Меню**: git → "Выполнить commit и push…"
- **Функционал**: 
  - Выполняет `git add --all`
  - Создает commit с указанным комментарием
  - Выполняет `git push`
- **Работает в**: Папке с тест-кейсами (должна быть git репозиторием)

#### Настройка интерфейса
- **Меню**: Вид → "Настроить ширины панелей…"
- **Функционал**: Настройка ширины панелей (дерево, форма, боковая панель)
- **Сохранение**: Настройки сохраняются в `settings.json`

---

## 📖 Инструкции по использованию

### Первый запуск

1. Запустите приложение: `python run_app_v2.py`
2. При первом запуске выберите папку с тест-кейсами:
   - Меню → Файл → "📁 Выбрать папку с тест-кейсами" (`Ctrl+O`)
   - Или используйте диалог выбора папки
3. Папка сохраняется в настройках для последующих запусков

### Создание нового тест-кейса

1. ПКМ на папке или корне дерева
2. Выберите "➕ Создать тест-кейс"
3. Новый тест-кейс создается с одним пустым шагом
4. Автоматически открывается форма редактирования
5. Заполните необходимые поля
6. Нажмите "💾 Сохранить"

### Редактирование тест-кейса

1. Кликните на тест-кейс в дереве
2. Форма редактирования откроется справа
3. Отредактируйте нужные поля:
   - Название: редактируется в заголовке
   - Основные поля: заполните все необходимые
   - Теги: по одному тегу на строку
   - Описание, Предусловия, Ожидаемый результат: многострочные поля
   - Шаги: используйте контекстное меню (ПКМ) для управления
4. Кнопка "💾 Сохранить" появится автоматически при изменении
5. Нажмите "💾 Сохранить" для сохранения изменений

### Работа со шагами

#### Добавление шага
1. ПКМ на списке шагов (в пустом месте) → "➕ Добавить новый шаг"
2. Или ПКМ на существующем шаге → "⬇️ Вставить шаг ниже"
3. Автоматически прокручивается к новому шагу и устанавливается фокус на поле "Действия"

#### Редактирование шага
1. Введите текст в поле "Действие"
2. Введите ожидаемый результат в поле "Ожидаемый результат"
3. Поля автоматически подстраивают высоту под содержимое

#### Управление шагами
- **Перемещение**: ПКМ на шаге → "⇡ Переместить наверх" / "⇣ Переместить вниз"
- **Удаление**: ПКМ на шаге → "✕ Удалить"
- **Вставка**: ПКМ на шаге → "⬆️ Вставить шаг выше" / "⬇️ Вставить шаг ниже"

### Работа в режиме запуска тестов

1. Переключите toggle switch в хеддере в положение "Запуск тестов"
2. Выберите тест-кейс в дереве
3. Для каждого шага установите статус:
   - ✓ (зеленый) - passed
   - ✕ (красный) - failed
   - S (серый) - skipped
4. Статусы автоматически сохраняются в JSON файл
5. Просмотрите статистику в панели "Статистика"

### Генерация Allure отчетов

1. Откройте панель "Статистика"
2. Нажмите кнопку "Generate Allure"
3. В папке приложения создастся папка `Reports/YYYY_MM_DD_HH_MM/`
4. В папке будут JSON файлы в формате Allure Test Result
5. Проводник автоматически откроется в созданной папке
6. Используйте команду для генерации отчета:
   ```bash
   allure serve <путь_к_папке>
   ```

### Ревью тест-кейса с LLM

1. Выберите тест-кейс в дереве
2. ПКМ на тест-кейсе → "📝 Ревью"
3. Или откройте панель "Ревью" вручную
4. Введите промт для ревью (или используйте сохраненный)
5. Нажмите Enter или кнопку отправки
6. Дождитесь ответа от LLM
7. Промт автоматически сохраняется для следующего использования

### Создание тест-кейсов с LLM

1. Откройте панель "Создать ТК"
2. (Опционально) Прикрепите файлы с техническим заданием
3. Введите задачу для создания тест-кейсов
4. Нажмите Enter или кнопку отправки
5. Дождитесь ответа от LLM
6. Тест-кейсы автоматически создадутся в папке "from LLM"
7. Промт автоматически сохраняется для следующего использования

### Массовые операции

1. Включите режим редактирования (если доступен)
2. Отметьте нужные элементы чекбоксами
3. Перетащите на папку для перемещения
4. Или нажмите кнопку удаления для массового удаления

### Поиск тест-кейсов

1. Введите текст в поле поиска в левой панели
2. Дерево автоматически фильтруется по введенному тексту
3. Очистите поле для отображения всех элементов

---

## 🏗️ Архитектура

### Структура проекта

```
test_case_editor/
├── models/                    # Модели данных
│   └── test_case.py          # Модели TestCase и TestCaseStep
├── repositories/             # Работа с файлами
│   └── test_case_repository.py  # Репозиторий для работы с JSON файлами
├── services/                 # Бизнес-логика
│   └── test_case_service.py  # Сервис для работы с тест-кейсами
├── ui/                       # Пользовательский интерфейс
│   ├── main_window.py        # Главное окно приложения
│   ├── widgets/              # Виджеты интерфейса
│   │   ├── tree_widget.py    # Дерево тест-кейсов
│   │   ├── form_widget.py    # Форма редактирования
│   │   ├── auxiliary_panel.py # Боковая панель (Ревью, Создать ТК, JSON, Статистика)
│   │   ├── review_panel.py   # Панель ревью
│   │   ├── stats_panel.py    # Панель статистики
│   │   ├── json_preview_widget.py # Просмотр JSON
│   │   └── toggle_switch.py  # Переключатель режимов
│   └── styles/               # Стили интерфейса
│       ├── app_theme.py      # Тема приложения
│       └── ui_metrics.py     # Метрики UI (шрифты, отступы)
└── utils/                    # Утилиты
    ├── allure_generator.py   # Генерация Allure отчетов
    ├── llm.py                # Работа с LLM
    ├── prompt_builder.py     # Построение промптов
    ├── azure_parser.py       # Парсинг Azure DevOps
    └── datetime_utils.py     # Утилиты для работы с датами
```

### Принципы SOLID

Приложение построено с применением принципов SOLID:

1. **Single Responsibility Principle (SRP)**
   - Каждый класс отвечает за одну задачу
   - `TestCase` - хранение данных
   - `TestCaseRepository` - работа с файлами
   - `TestCaseService` - бизнес-логика
   - `MainWindow` - координация UI

2. **Open/Closed Principle (OCP)**
   - Можно добавлять новые виджеты без изменения существующих
   - Можно создать новый репозиторий (например, для БД) без изменения сервиса

3. **Liskov Substitution Principle (LSP)**
   - Любая реализация `ITestCaseRepository` может заменить `TestCaseRepository`

4. **Interface Segregation Principle (ISP)**
   - `ITestCaseRepository` определяет только необходимые методы
   - Клиенты зависят только от нужных им интерфейсов

5. **Dependency Inversion Principle (DIP)**
   - `MainWindow` зависит от `TestCaseService` (абстракция)
   - `TestCaseService` зависит от `ITestCaseRepository` (абстракция)
   - Конкретные реализации создаются на верхнем уровне

---

## 🔧 Технические детали

### Формат данных

Тест-кейсы хранятся в JSON файлах со следующей структурой:

```json
{
  "id": "uuid",
  "name": "Название тест-кейса",
  "description": "Описание",
  "preconditions": "Предусловия",
  "expectedResult": "Общий ожидаемый результат",
  "epic": "Epic",
  "feature": "Feature",
  "story": "Story",
  "component": "Component",
  "testLayer": "E2E",
  "severity": "NORMAL",
  "priority": "MEDIUM",
  "environment": "dev",
  "browser": "chrome",
  "owner": "Owner",
  "author": "Author",
  "reviewer": "Reviewer",
  "testCaseId": "TC-001",
  "issueLinks": "ISSUE-1, ISSUE-2",
  "testCaseLinks": "TC-002, TC-003",
  "tags": "tag1, tag2",
  "testType": "manual",
  "status": "Draft",
  "steps": [
    {
      "id": "uuid",
      "name": "Название шага",
      "description": "Действие",
      "expectedResult": "Ожидаемый результат",
      "status": "pending",
      "bugLink": "",
      "skipReason": "",
      "attachments": ""
    }
  ],
  "createdAt": 1234567890000,
  "updatedAt": 1234567890000
}
```

### Настройки

Настройки хранятся в файле `settings.json` в корне проекта:

```json
{
  "test_cases_dir": "путь/к/папке/с/тест-кейсами",
  "DEFAULT_PROMT": "Промт для ревью",
  "DEFAULT_PROMT_CREATE_TC": "Промт для создания ТК",
  "LLM_MODEL": "модель",
  "LLM_HOST": "http://host:port",
  "LLM_METHODIC_PATH": "путь/к/методике.md",
  "panel_sizes": {
    "left": 350,
    "form_area": 900,
    "review": 360
  },
  "window_geometry": {
    "x": 100,
    "y": 100,
    "width": 1600,
    "height": 900,
    "is_fullscreen": false
  }
}
```

### Горячие клавиши

- `Ctrl+O` - Выбрать папку с тест-кейсами
- `Ctrl+Q` - Выход из приложения

### Зависимости

- **PyQt5** >= 5.15.0 - GUI фреймворк

Установка:
```bash
pip install -r requirements.txt
```

---

## 📝 Примечания

- Все данные хранятся в JSON файлах
- Формат данных совместим между версиями
- Настройки автоматически сохраняются
- Состояние интерфейса (размеры панелей, позиция окна) сохраняется между сеансами
- Дерево тест-кейсов сохраняет состояние развернутых папок при сохранении
